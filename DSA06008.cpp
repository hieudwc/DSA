#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define pb push_back
#define mp make_pair
#define is insert

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while(t--){
        int n, m;
        cin >> n >> m;
        int a[n], b[m], c[n+m];
        int k = 0;
        for(int i = 0; i < n; i++){
            cin >> a[i];
            c[k]=a[i];
            k++;
        }
        for(int i = 0; i < m; i++){
            cin >> b[i];
            c[k]=b[i];
            k++
        }
        int cnt = 0;
        for(int i = 0; i < k; i++){
            while(l<r)
        }
        cout << cnt << endl;
    }
}